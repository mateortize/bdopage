= tabs_tag :open_tabs=>{ class: 'nav nav-tabs' } do |tab|
  = tab.all 'All', admin_posts_path
  = tab.published 'Published', admin_posts_path(status: 'published')
  = tab.draft 'Draft', admin_posts_path(status: 'draft')
  = tab.trash 'Trash', admin_posts_path(status: 'trash')